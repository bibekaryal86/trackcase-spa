import React from 'react'

const TaskCalendar = (): React.ReactElement => {
  const pageText = () => (
    <>
      <h5>This is the TaskCalendar!</h5>
    </>
  )

  return <>{pageText()}</>
}

export default TaskCalendar
