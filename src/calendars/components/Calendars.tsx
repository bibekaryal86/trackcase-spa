import React from 'react'

const Calendars = (): React.ReactElement => {
  const pageText = () => (
    <>
      <h5>This is the Calendar!</h5>
    </>
  )

  return <>{pageText()}</>
}

export default Calendars