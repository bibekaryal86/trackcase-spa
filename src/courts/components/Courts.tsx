import React from 'react'

const Courts = (): React.ReactElement => {
  const pageText = () => (
    <>
      <h5>This is the Courts!</h5>
    </>
  )

  return <>{pageText()}</>
}

export default Courts
