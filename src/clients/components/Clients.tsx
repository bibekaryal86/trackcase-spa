import React from 'react'

const Clients = (): React.ReactElement => {
  const pageText = () => (
    <>
      <h5>This is the Clients!</h5>
    </>
  )

  return <>{pageText()}</>
}

export default Clients
