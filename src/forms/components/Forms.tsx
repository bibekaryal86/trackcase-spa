import React from 'react'

const Forms = (): React.ReactElement => {
  const pageText = () => (
    <>
      <h5>This is the Forms!</h5>
    </>
  )

  return <>{pageText()}</>
}

export default Forms
