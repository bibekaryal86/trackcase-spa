import React from 'react'

const UserAdmin = (): React.ReactElement => {
  const pageText = () => (
    <>
      <h5>This is the UserAdmin page!</h5>
    </>
  )

  return <>{pageText()}</>
}

export default UserAdmin
