import React from 'react'

const HearingCalendar = (): React.ReactElement => {
  const pageText = () => (
    <>
      <h5>This is the HearingCalendar!</h5>
    </>
  )

  return <>{pageText()}</>
}

export default HearingCalendar
