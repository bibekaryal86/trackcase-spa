import React from 'react'

const HearingCalendars = (): React.ReactElement => {
  const pageText = () => (
    <>
      <h5>This is the HearingCalendars!</h5>
    </>
  )

  return <>{pageText()}</>
}

export default HearingCalendars
