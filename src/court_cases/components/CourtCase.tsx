import React from 'react'

const CourtCase = (): React.ReactElement => {
  const pageText = () => (
    <>
      <h5>This is the CourtCase!</h5>
    </>
  )

  return <>{pageText()}</>
}

export default CourtCase
